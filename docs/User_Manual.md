# User Manual (v1)

## Installation
- Create venv
- pip install -r requirements.txt

## Usage
- Run rules baseline:
  `python scripts/run_rules_baseline.py --data <path.jsonl> --out_dir runs/<name>`
- Outputs: predictions + metrics + config
